import { Progress as AntdProgress } from 'antd-v5';
import { withTheme } from '../../withTheme';

const Progress = withTheme(AntdProgress);

export default Progress;
export type { ProgressProps } from 'antd-v5';
