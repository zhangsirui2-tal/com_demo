import { Layout as AntdLayout } from 'antd-v5';
import { withTheme } from '../../withTheme';

const Layout = withTheme(AntdLayout);

export default Layout;
export type { LayoutProps } from 'antd-v5';
