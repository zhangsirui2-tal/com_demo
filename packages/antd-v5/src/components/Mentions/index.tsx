import { Mentions as AntdMentions } from 'antd-v5';
import { withTheme } from '../../withTheme';

const Mentions = withTheme(AntdMentions);

export default Mentions;
export type { MentionsProps } from 'antd-v5';
