import { Switch as AntdSwitch } from 'antd-v5';
import { withTheme } from '../../withTheme';

const Switch = withTheme(AntdSwitch);

export default Switch;
export type { SwitchProps } from 'antd-v5';
