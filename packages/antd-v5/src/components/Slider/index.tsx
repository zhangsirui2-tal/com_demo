import { Slider as AntdSlider } from 'antd-v5';
import { withTheme } from '../../withTheme';

const Slider = withTheme(AntdSlider);

export default Slider;
export type { SliderProps } from 'antd-v5';
