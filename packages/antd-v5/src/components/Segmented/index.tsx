import { Segmented as AntdSegmented } from 'antd-v5';
import { withTheme } from '../../withTheme';

const Segmented = withTheme(AntdSegmented);

export default Segmented;
export type { SegmentedProps } from 'antd-v5';
