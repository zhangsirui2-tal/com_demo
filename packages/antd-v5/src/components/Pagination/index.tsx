import { Pagination as AntdPagination } from 'antd-v5';
import { withTheme } from '../../withTheme';

const Pagination = withTheme(AntdPagination);

export default Pagination;
export type { PaginationProps } from 'antd-v5';
