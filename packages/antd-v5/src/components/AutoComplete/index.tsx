import { AutoComplete as AntdAutoComplete } from 'antd-v5';
import { withTheme } from '../../withTheme';

const AutoComplete = withTheme(AntdAutoComplete);

export default AutoComplete;
export type { AutoCompleteProps } from 'antd-v5';
