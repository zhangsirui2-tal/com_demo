import { Button as AntdButton } from 'antd-v5';
import { withTheme } from '../../withTheme';

const Button = withTheme(AntdButton);

export default Button;
export type { ButtonProps } from 'antd-v5';
