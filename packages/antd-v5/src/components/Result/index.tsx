import { Result as AntdResult } from 'antd-v5';
import { withTheme } from '../../withTheme';

const Result = withTheme(AntdResult);

export default Result;
export type { ResultProps } from 'antd-v5';
