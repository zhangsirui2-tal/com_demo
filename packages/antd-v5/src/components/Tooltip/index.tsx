import { Tooltip as AntdTooltip } from 'antd-v5';
import { withTheme } from '../../withTheme';

const Tooltip = withTheme(AntdTooltip);

export default Tooltip;
export type { TooltipProps } from 'antd-v5';
