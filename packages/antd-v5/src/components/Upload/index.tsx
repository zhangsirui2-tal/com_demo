import { Upload as AntdUpload } from 'antd-v5';
import { withTheme } from '../../withTheme';

const Upload = withTheme(AntdUpload);

export default Upload;
export type { UploadProps } from 'antd-v5';
