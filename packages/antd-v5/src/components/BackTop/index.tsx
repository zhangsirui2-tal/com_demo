import { BackTop as AntdBackTop } from 'antd-v5';
import { withTheme } from '../../withTheme';

const BackTop = withTheme(AntdBackTop);

export default BackTop;
export type { BackTopProps } from 'antd-v5';
