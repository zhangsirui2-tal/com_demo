import { Collapse as AntdCollapse } from 'antd-v5';
import { withTheme } from '../../withTheme';

const Collapse = withTheme(AntdCollapse);

export default Collapse;
export type { CollapseProps } from 'antd-v5';
