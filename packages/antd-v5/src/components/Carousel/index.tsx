import { Carousel as AntdCarousel } from 'antd-v5';
import { withTheme } from '../../withTheme';

const Carousel = withTheme(AntdCarousel);

export default Carousel;
export type { CarouselProps } from 'antd-v5';
