import { Popover as AntdPopover } from 'antd-v5';
import { withTheme } from '../../withTheme';

const Popover = withTheme(AntdPopover);

export default Popover;
export type { PopoverProps } from 'antd-v5';
